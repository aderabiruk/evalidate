/**
 * String Type Error Message
 * 
 * @param {String} field 
 */
export const string_type_error_message = (field: string): string => {
    return `${field} must be a string`;
};

/**
 * Array Type Error Message
 * 
 * @param {String} field 
 */
export const array_type_error_message = (field: string): string => {
    return `${field} must be an array`;
};

/**
 * Integer Type Error Message
 * 
 * @param {String} field 
 */
export const integer_type_error_message = (field: string): string => {
    return `${field} must be an integer`;
};

/**
 * Boolean Type Error Message
 * 
 * @param {String} field 
 */
export const boolean_type_error_message = (field: string): string => {
    return `${field} must be a boolean`;
};

/**
 * Date Type Error Message
 * 
 * @param {String} field 
 */
export const date_type_error_message = (field: string): string => {
    return `${field} must be a date`;
};

/**
 * Number Type Error Message
 * 
 * @param {String} field 
 */
export const number_type_error_message = (field: string): string => {
    return `${field} must be a number`;
};