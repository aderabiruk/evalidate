/**
 * Equal Error Message
 */
export const boolean_equal_error_message = (): string => {
    return "Invalid value provided for ${{}}";
};

/**
 * Required Error Message
 */
export const boolean_required_error_message = (): string => {
    return `\${{}} is required`;
};